<!DOCTYPE html>
<html>
  <head>
    <title>  </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  <div class="container-fluid">
    <div class="home-heading text-center row">
      <div class="home-heading-wrapper">
        <h1>Manda </h1>
        <p>create, share and control your favorites recipe</p>
       <a class="btn btn-default" href="#">join a comunity full of pasion and comradership</a>


   <%  if ( typeof pictures !== "undefined"){ %>
          <section>
           <% pictures.forEach(function(picture) { %>
             <h3><%= picture.name %></h3>
             <img src="<%= picture.pic_path %>" alt="<%= picture.pic_name %>" />
           <% })%>
             </section>
      <%  } %>
      </div>
    </div>
  </div>
  </body>
</html>
